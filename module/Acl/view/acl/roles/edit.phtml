<article id="roles" class="page">
    <header>
        <h1>Editar Papel</h1>
    </header>

<?php
$form = $this->form; 

$form->setAttribute('action', $this->url('acl-admin/default',array('controller'=>'roles','action'=>'edit')));

$form->prepare();
echo $this->form()->openTag($form);

echo "<fieldset><legend>Altere o formulário abaixo.</legend>";
$formContent="";
foreach ($form as $element){
        $formContent .= "\n<div class=\"pure-controls\">".$this->formrow($element);
        if(strpos($this->formrow($element), "type=\"submit\"")>0){
                $formContent .= " <a href=\"". $this->url('acl-admin/default',array('controller'=>'roles')) ."\" class=\"pure-button button-secondary\">Cancelar</a>";
        }
        $formContent .=  "</div>";
        
}
echo $formContent;
echo "</fieldset>";
echo $this->form()->closeTag();

?>
</article>